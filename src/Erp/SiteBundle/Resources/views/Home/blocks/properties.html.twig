<section id="properties" class="properties">
    <div class="prop-title">
        <h2 class="bold-text">Available Properties</h2>
        <span class="fa fa-chevron-circle-left "></span><span class="fa fa-chevron-circle-right"></span>
    </div>

    <div class="container">
        <div class="row carousel-property">

            {% for property in properties %}
                {% include 'ErpPropertyBundle:Property:property-card.html.twig' with {'isForHome' : true} %}
            {% endfor %}
        </div>
    </div>

</section>
